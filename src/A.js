import styled from 'styled-components'

const A = styled.a`
    color: #16BFFD;
    text-decoration: none;
    cursor: crosshair;
    &:hover {
        text-decoration: underline;
    }
`

export default A
